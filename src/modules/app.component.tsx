import React, { Component } from 'react';
import './app.scss';
import InitContainer from '@modules/init/init.container';

class App extends Component {
    render() {
        return (
            <InitContainer/>
        );
    }
}
export default App;
